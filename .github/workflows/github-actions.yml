name: Node.js CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: zlv
        ports:
          - 5432:5432
      postgres_test:
        image: postgres
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_zlv
        ports:
          - 5433:5433
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Server install
        run: npm install
      - name: Server tests
        run: npm run test
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5433/test_zlv
      - name : build
        run: npm run build
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/zlv
      - name: Frontend tests
        run: npm run frontend:test
