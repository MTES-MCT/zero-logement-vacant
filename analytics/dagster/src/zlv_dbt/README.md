# `src/zlv_dbt/` — unused dbt scaffold

This directory was generated by `dagster-dbt project scaffold` and is **not**
registered by `analytics/dagster/workspace.yaml` (which loads only
`python_module: src`).

The active dbt assets live in
[`src/assets/production_dbt.py`](../assets/production_dbt.py) and are wired
into the main `Definitions` block at [`src/definitions.py`](../definitions.py).

If you intend to migrate Dagster's dbt orchestration here:

1. Add this directory to `workspace.yaml` as a separate `python_module` or
   `python_file` entry.
2. Remove the assets in `src/assets/production_dbt.py` to avoid double
   materialization.
3. Update the schedule in `src/definitions.py` accordingly.

Otherwise, this scaffold can be deleted in a follow-up cleanup.
