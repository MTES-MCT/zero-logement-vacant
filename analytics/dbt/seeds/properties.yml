version: 2

seeds:
  - name: com_epci_dep_reg
    config:
      delimiter: ";"
      +quote: '"'  # Add this to handle quoted fields
      column_types:
        reg_siren: varchar  # Change from INTEGER to varchar to handle empty strings
        
  - name: taxe
    config:
      delimiter: ";"
      +quote: '"'
      +header: true
      enabled: false
  - name: nature_juridique
    config:
      delimiter: ";"
      +quote: '"'
      +header: true
      enabled: false
  - name: direction_territoriale
    config:
      delimiter: ";"
  - name: prenom
    config:
      delimiter: ","

  - name: tlv_cities
    config:
      delimiter: ";"
      +header: true

  - name: departement_france
    config:
      delimiter: ","
      +header: true

  - name: cities
    config:
      delimiter: ","
      +header: true

  - name: article_232_1
    config:
      +header: true

  - name: article_232_2
    config:
      +header: true


  - name: ngeo-anct-cog2023
    config:
      +header: true

  - name: opah
    config:
      +header: true
      delimiter: ","
      +quote: '"'  # Add this to handle quoted fields in the name
      strict_mode: false  # Add this to handle potential CSV format issues

  - name: ort
    delimiter: ","
    config:
      +header: true
      column_types:
          "Si signée, date de signature": varchar(5)

  - name: table_appartenance_geo_communes_2024
    config:
      delimiter: ","
      +header: true
      +quote: '"'
      strict_mode: false
      ignore_errors: false
  
  - name: fusions
    description: "Fusions de communes"
    config:
      delimiter: ","
      +header: true

  - name: scissions
    config:
      delimiter: ","
      +header: true