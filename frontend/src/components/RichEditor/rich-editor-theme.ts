import { EditorThemeClasses } from 'lexical';

const theme: EditorThemeClasses = {
  root: 'fr-input editor',
  text: {
    bold: 'editor__text--bold',
    italic: 'editor__text--italic',
    underline: 'editor__text--underline',
  },
};

export default theme;
