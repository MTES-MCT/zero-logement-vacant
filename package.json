{
  "name": "zero-logement-vacant",
  "version": "1.0.0",
  "description": "Zéro Logement Vacant",
  "main": "server/index.ts",
  "scripts": {
    "frontend:build": "cd frontend && npm install && npm run build",
    "frontend:start": "cd frontend && npm start",
    "server-local": "ts-node-dev --respawn --transpile-only -r dotenv/config server/index.ts",
    "start-local": "run-p frontend:start server-local",
    "install": "npm run frontend:build",
    "start": "node server/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^16.9.1",
    "airtable": "^0.11.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "ts-node-dev": "^1.1.8"
  },
  "engines": {
    "node": "^14.16.0",
    "npm": "^6.14.0"
  }
}
