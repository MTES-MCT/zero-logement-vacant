<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#000000" />
    <title>Zéro Logement Vacant</title>
  </head>
  <body>
    {{#if watermark}}
    <div class="watermark">Prévisualisation</div>
    {{/if}}
    <noscript>You need to enable JavaScript to run this app.</noscript>
    <header class="header">
      <section class="header--left">
        {{#each logo}}
          <img alt="logo" class="header__image" src="{{this}}" />
        {{/each}}
      </section>
      <section class="header--right">
        <section class="header__sender">
          <p>{{sender.name}}</p>
          <p>{{sender.service}}</p>
          <p>{{sender.firstName}} {{sender.lastName}}</p>
          <p>{{sender.address}}</p>
          <p>{{sender.email}}</p>
          <p>{{sender.phone}}</p>
        </section>
        <section class="header__recipient">
          <strong>À l’attention de</strong>
          <p>{{owner.fullName}}</p>
          {{#each owner.address}}
            <p>{{this}}</p>
          {{/each}}
        </section>
      </section>
    </header>
    <main>
      <header class="main__header">
        <p>À {{writtenFrom}}, le {{localdate writtenAt}}</p>
        <p><strong>Objet : </strong>{{subject}}</p>
      </header>
      <section class="main__body">
        {{{body}}}
      </section>
    </main>
    <footer>
      <section class="footer__signature">
        <p><img src="{{sender.signatoryFile}}" alt="Signature" /></p>
        <p>{{sender.signatoryFirstName}} {{sender.signatoryLastName}}</p>
        <p>{{sender.signatoryRole}}</p>
      </section>
    </footer>
  </body>
</html>
